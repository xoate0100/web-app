import { ActivateSavingsAccountComponent } from './activate-savings-account.component';

describe('ActivateSavingsAccountComponent', () => {
  it('should create', () => {
    expect(ActivateSavingsAccountComponent).toBeDefined();
  });
});
