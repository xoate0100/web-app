import { SavingAccountActionsComponent } from './saving-account-actions.component';

describe('SavingAccountActionsComponent', () => {
  it('should create', () => {
    expect(SavingAccountActionsComponent).toBeDefined();
  });
});
