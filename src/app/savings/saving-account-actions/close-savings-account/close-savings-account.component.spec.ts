import { CloseSavingsAccountComponent } from './close-savings-account.component';

describe('CloseSavingsAccountComponent', () => {
  it('should create', () => {
    expect(CloseSavingsAccountComponent).toBeDefined();
  });
});
