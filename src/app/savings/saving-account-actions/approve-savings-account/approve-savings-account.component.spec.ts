import { ApproveSavingsAccountComponent } from './approve-savings-account.component';

describe('ApproveSavingsAccountComponent', () => {
  it('should create', () => {
    expect(ApproveSavingsAccountComponent).toBeDefined();
  });
});
