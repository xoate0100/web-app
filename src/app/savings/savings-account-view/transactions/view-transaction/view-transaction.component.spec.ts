import { ViewTransactionComponent } from './view-transaction.component';

describe('ViewTransactionComponent', () => {
  it('should create', () => {
    expect(ViewTransactionComponent).toBeDefined();
  });
});
