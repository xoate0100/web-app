import { SavingsAccountViewComponent } from './savings-account-view.component';

describe('SavingsAccountViewComponent', () => {
  it('should create', () => {
    expect(SavingsAccountViewComponent).toBeDefined();
  });
});
