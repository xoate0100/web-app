import { AppConfigurationComponent } from './app-configuration.component';

describe('AppConfigurationComponent', () => {
  it('should create', () => {
    expect(AppConfigurationComponent).toBeDefined();
  });
});
