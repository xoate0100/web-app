import { ChangePasswordDialogComponent } from './change-password-dialog.component';

describe('ChangePasswordDialogComponent', () => {
  it('should create', () => {
    expect(ChangePasswordDialogComponent).toBeDefined();
  });
});
