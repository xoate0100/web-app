import { KeyboardShortcutsDialogComponent } from './keyboard-shortcuts-dialog.component';

describe('KeyboardShortcutsDialogComponent', () => {
  it('should create', () => {
    expect(KeyboardShortcutsDialogComponent).toBeDefined();
  });
});
