import { LanguageSelectorComponent } from './language-selector.component';

describe('LanguageSelectorComponent', () => {
  it('should create', () => {
    expect(LanguageSelectorComponent).toBeDefined();
  });
});
