import { DisableDialogComponent } from './disable-dialog.component';

describe('DisableDialogComponent', () => {
  it('should create', () => {
    expect(DisableDialogComponent).toBeDefined();
  });
});
