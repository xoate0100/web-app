import { ServerSelectorComponent } from './server-selector.component';

describe('ServerSelectorComponent', () => {
  it('should create', () => {
    expect(ServerSelectorComponent).toBeDefined();
  });
});
