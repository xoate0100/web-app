import { NotificationsTrayComponent } from './notifications-tray.component';

describe('NotificationsTrayComponent', () => {
  it('should create', () => {
    expect(NotificationsTrayComponent).toBeDefined();
  });
});
