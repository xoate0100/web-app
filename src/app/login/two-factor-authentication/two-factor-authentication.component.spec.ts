import { TwoFactorAuthenticationComponent } from './two-factor-authentication.component';

describe('TwoFactorAuthenticationComponent', () => {
  it('should create', () => {
    expect(TwoFactorAuthenticationComponent).toBeDefined();
  });
});
