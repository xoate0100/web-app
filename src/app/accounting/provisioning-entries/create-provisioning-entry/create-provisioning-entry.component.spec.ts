import { CreateProvisioningEntryComponent } from './create-provisioning-entry.component';

describe('CreateProvisioningEntryComponent', () => {
  it('should create', () => {
    expect(CreateProvisioningEntryComponent).toBeDefined();
  });
});
