import { AccountingRulesComponent } from './accounting-rules.component';

describe('AccountingRulesComponent', () => {
  it('should create', () => {
    expect(AccountingRulesComponent).toBeDefined();
  });
});
