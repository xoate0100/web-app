import { CreateGlAccountComponent } from './create-gl-account.component';

describe('CreateGlAccountComponent', () => {
  it('should create', () => {
    expect(CreateGlAccountComponent).toBeDefined();
  });
});
