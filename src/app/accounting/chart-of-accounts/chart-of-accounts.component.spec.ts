import { ChartOfAccountsComponent } from './chart-of-accounts.component';

describe('ChartOfAccountsComponent', () => {
  it('should create', () => {
    expect(ChartOfAccountsComponent).toBeDefined();
  });
});
