import { CreateClosureComponent } from './create-closure.component';

describe('CreateClosureComponent', () => {
  it('should create', () => {
    expect(CreateClosureComponent).toBeDefined();
  });
});
