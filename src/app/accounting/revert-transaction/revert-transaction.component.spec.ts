import { RevertTransactionComponent } from './revert-transaction.component';

describe('RevertTransactionComponent', () => {
  it('should create', () => {
    expect(RevertTransactionComponent).toBeDefined();
  });
});
