import { ActivateCenterComponent } from './activate-center.component';

describe('ActivateCenterComponent', () => {
  it('should create', () => {
    expect(ActivateCenterComponent).toBeDefined();
  });
});
