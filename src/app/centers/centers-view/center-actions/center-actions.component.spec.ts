import { CenterActionsComponent } from './center-actions.component';

describe('CenterActionsComponent', () => {
  it('should create', () => {
    expect(CenterActionsComponent).toBeDefined();
  });
});
