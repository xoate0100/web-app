import { CheckerInboxAndTasksComponent } from './checker-inbox-and-tasks.component';

describe('CheckerInboxAndTasksComponent', () => {
  it('should create', () => {
    expect(CheckerInboxAndTasksComponent).toBeDefined();
  });
});
