import { ClientApprovalComponent } from './client-approval.component';

describe('ClientApprovalComponent', () => {
  it('should create', () => {
    expect(ClientApprovalComponent).toBeDefined();
  });
});
