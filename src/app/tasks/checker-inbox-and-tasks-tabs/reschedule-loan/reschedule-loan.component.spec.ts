import { RescheduleLoanComponent } from './reschedule-loan.component';

describe('RescheduleLoanComponent', () => {
  it('should create', () => {
    expect(RescheduleLoanComponent).toBeDefined();
  });
});
