import { ViewCheckerInboxComponent } from './view-checker-inbox.component';

describe('ViewCheckerInboxComponent', () => {
  it('should create', () => {
    expect(ViewCheckerInboxComponent).toBeDefined();
  });
});
