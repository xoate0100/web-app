import { ListTransactionsComponent } from './list-transactions.component';

describe('ListTransactionsComponent', () => {
  it('should create', () => {
    expect(ListTransactionsComponent).toBeDefined();
  });
});
