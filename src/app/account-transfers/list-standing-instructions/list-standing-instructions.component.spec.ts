import { ListStandingInstructionsComponent } from './list-standing-instructions.component';

describe('ListStandingInstructionsComponent', () => {
  it('should create', () => {
    expect(ListStandingInstructionsComponent).toBeDefined();
  });
});
