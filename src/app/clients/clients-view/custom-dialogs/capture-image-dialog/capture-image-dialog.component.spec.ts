import { CaptureImageDialogComponent } from './capture-image-dialog.component';

describe('CaptureImageDialogComponent', () => {
  it('should create', () => {
    expect(CaptureImageDialogComponent).toBeDefined();
  });
});
