import { DeleteSignatureDialogComponent } from './delete-signature-dialog.component';

describe('DeleteSignatureDialogComponent', () => {
  it('should create', () => {
    expect(DeleteSignatureDialogComponent).toBeDefined();
  });
});
