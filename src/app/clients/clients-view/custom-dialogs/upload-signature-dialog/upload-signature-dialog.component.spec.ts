import { UploadSignatureDialogComponent } from './upload-signature-dialog.component';

describe('UploadSignatureDialogComponent', () => {
  it('should create', () => {
    expect(UploadSignatureDialogComponent).toBeDefined();
  });
});
