import { ViewSignatureDialogComponent } from './view-signature-dialog.component';

describe('ViewSignatureDialogComponent', () => {
  it('should create', () => {
    expect(ViewSignatureDialogComponent).toBeDefined();
  });
});
