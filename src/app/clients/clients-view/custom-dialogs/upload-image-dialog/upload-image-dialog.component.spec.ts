import { UploadImageDialogComponent } from './upload-image-dialog.component';

describe('UploadImageDialogComponent', () => {
  it('should create', () => {
    expect(UploadImageDialogComponent).toBeDefined();
  });
});
