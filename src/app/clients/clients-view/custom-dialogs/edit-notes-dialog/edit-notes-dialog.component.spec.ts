import { EditNotesDialogComponent } from './edit-notes-dialog.component';

describe('EditNotesDialogComponent', () => {
  it('should create', () => {
    expect(EditNotesDialogComponent).toBeDefined();
  });
});
