import { IdentitiesTabComponent } from './identities-tab.component';

describe('IdentitiesTabComponent', () => {
  it('should create', () => {
    expect(IdentitiesTabComponent).toBeDefined();
  });
});
