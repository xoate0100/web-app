import { UndoClientRejectionComponent } from './undo-client-rejection.component';

describe('UndoClientRejectionComponent', () => {
  it('should create', () => {
    expect(UndoClientRejectionComponent).toBeDefined();
  });
});
