import { ClientActionsComponent } from './client-actions.component';

describe('ClientActionsComponent', () => {
  it('should create', () => {
    expect(ClientActionsComponent).toBeDefined();
  });
});
