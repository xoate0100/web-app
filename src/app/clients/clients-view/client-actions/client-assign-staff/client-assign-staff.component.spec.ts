import { ClientAssignStaffComponent } from './client-assign-staff.component';

describe('ClientAssignStaffComponent', () => {
  it('should create', () => {
    expect(ClientAssignStaffComponent).toBeDefined();
  });
});
