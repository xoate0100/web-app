import { AcceptClientTransferComponent } from './accept-client-transfer.component';

describe('AcceptClientTransferComponent', () => {
  it('should create', () => {
    expect(AcceptClientTransferComponent).toBeDefined();
  });
});
