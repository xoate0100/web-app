import { ReactivateClientComponent } from './reactivate-client.component';

describe('ReactivateClientComponent', () => {
  it('should create', () => {
    expect(ReactivateClientComponent).toBeDefined();
  });
});
