import { ClientAddressStepComponent } from './client-address-step.component';

describe('ClientAddressStepComponent', () => {
  it('should create', () => {
    expect(ClientAddressStepComponent).toBeDefined();
  });
});
