import { ClientGeneralStepComponent } from './client-general-step.component';

describe('ClientGeneralStepComponent', () => {
  it('should create', () => {
    expect(ClientGeneralStepComponent).toBeDefined();
  });
});
