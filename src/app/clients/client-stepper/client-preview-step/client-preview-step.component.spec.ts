import { ClientPreviewStepComponent } from './client-preview-step.component';

describe('ClientPreviewStepComponent', () => {
  it('should create', () => {
    expect(ClientPreviewStepComponent).toBeDefined();
  });
});
