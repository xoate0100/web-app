import { AmountCollectedPieComponent } from './amount-collected-pie.component';

describe('AmountCollectedPieComponent', () => {
  it('should create', () => {
    expect(AmountCollectedPieComponent).toBeDefined();
  });
});
