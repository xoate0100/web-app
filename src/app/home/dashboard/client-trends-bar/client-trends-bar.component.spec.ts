import { ClientTrendsBarComponent } from './client-trends-bar.component';

describe('ClientTrendsBarComponent', () => {
  it('should create', () => {
    expect(ClientTrendsBarComponent).toBeDefined();
  });
});
