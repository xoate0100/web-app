import { SharesAccountViewComponent } from './shares-account-view.component';

describe('SharesAccountViewComponent', () => {
  it('should create', () => {
    expect(SharesAccountViewComponent).toBeDefined();
  });
});
