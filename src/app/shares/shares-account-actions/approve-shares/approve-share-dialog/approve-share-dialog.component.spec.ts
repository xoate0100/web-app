import { ApproveShareDialogComponent } from './approve-share-dialog.component';

describe('ApproveShareDialogComponent', () => {
  it('should create', () => {
    expect(ApproveShareDialogComponent).toBeDefined();
  });
});
