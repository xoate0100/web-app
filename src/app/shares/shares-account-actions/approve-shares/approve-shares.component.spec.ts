import { ApproveSharesComponent } from './approve-shares.component';

describe('ApproveSharesComponent', () => {
  it('should create', () => {
    expect(ApproveSharesComponent).toBeDefined();
  });
});
