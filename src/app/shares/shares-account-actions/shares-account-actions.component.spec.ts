import { SharesAccountActionsComponent } from './shares-account-actions.component';

describe('SharesAccountActionsComponent', () => {
  it('should create', () => {
    expect(SharesAccountActionsComponent).toBeDefined();
  });
});
