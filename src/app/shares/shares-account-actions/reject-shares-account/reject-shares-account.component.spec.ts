import { RejectSharesAccountComponent } from './reject-shares-account.component';

describe('RejectSharesAccountComponent', () => {
  it('should create', () => {
    expect(RejectSharesAccountComponent).toBeDefined();
  });
});
