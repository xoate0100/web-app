import { RejectShareDialogComponent } from './reject-share-dialog.component';

describe('RejectSharesDialogComponent', () => {
  it('should create', () => {
    expect(RejectShareDialogComponent).toBeDefined();
  });
});
