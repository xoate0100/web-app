import { ActivateSharesAccountComponent } from './activate-shares-account.component';

describe('ActivateSharesAccountComponent', () => {
  it('should create', () => {
    expect(ActivateSharesAccountComponent).toBeDefined();
  });
});
