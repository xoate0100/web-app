import { CloseSharesAccountComponent } from './close-shares-account.component';

describe('CloseSharesAccountComponent', () => {
  it('should create', () => {
    expect(CloseSharesAccountComponent).toBeDefined();
  });
});
