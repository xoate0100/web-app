import { ApproveSharesAccountComponent } from './approve-shares-account.component';

describe('ApproveSharesAccountComponent', () => {
  it('should create', () => {
    expect(ApproveSharesAccountComponent).toBeDefined();
  });
});
