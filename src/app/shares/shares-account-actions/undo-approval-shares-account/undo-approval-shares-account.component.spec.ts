import { UndoApprovalSharesAccountComponent } from './undo-approval-shares-account.component';

describe('UndoApprovalSharesAccountComponent', () => {
  it('should create', () => {
    expect(UndoApprovalSharesAccountComponent).toBeDefined();
  });
});
