import { SharesAccountPreviewStepComponent } from './shares-account-preview-step.component';

describe('SharesAccountPreviewStepComponent', () => {
  it('should create', () => {
    expect(SharesAccountPreviewStepComponent).toBeDefined();
  });
});
