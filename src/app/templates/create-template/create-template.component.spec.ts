import { CreateTemplateComponent } from './create-template.component';

describe('CreateTemplateComponent', () => {
  it('should create', () => {
    expect(CreateTemplateComponent).toBeDefined();
  });
});
