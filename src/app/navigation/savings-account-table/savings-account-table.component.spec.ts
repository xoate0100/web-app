import { SavingsAccountTableComponent } from './savings-account-table.component';

describe('SavingsAccountTableComponent', () => {
  it('should create', () => {
    expect(SavingsAccountTableComponent).toBeDefined();
  });
});
