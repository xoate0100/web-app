import { StaffNavigationComponent } from './staff-navigation.component';

describe('StaffNavigationComponent', () => {
  it('should create', () => {
    expect(StaffNavigationComponent).toBeDefined();
  });
});
