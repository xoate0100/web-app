import { CenterNavigationComponent } from './center-navigation.component';

describe('CenterNavigationComponent', () => {
  it('should create', () => {
    expect(CenterNavigationComponent).toBeDefined();
  });
});
