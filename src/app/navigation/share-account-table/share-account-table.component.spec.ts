import { ShareAccountTableComponent } from './share-account-table.component';

describe('ShareAccountTableComponent', () => {
  it('should create', () => {
    expect(ShareAccountTableComponent).toBeDefined();
  });
});
