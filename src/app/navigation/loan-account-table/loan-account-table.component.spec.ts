import { LoanAccountTableComponent } from './loan-account-table.component';

describe('LoanAccountTableComponent', () => {
  it('should create', () => {
    expect(LoanAccountTableComponent).toBeDefined();
  });
});
