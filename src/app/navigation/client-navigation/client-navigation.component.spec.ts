import { ClientNavigationComponent } from './client-navigation.component';

describe('ClientNavigationComponent', () => {
  it('should create', () => {
    expect(ClientNavigationComponent).toBeDefined();
  });
});
