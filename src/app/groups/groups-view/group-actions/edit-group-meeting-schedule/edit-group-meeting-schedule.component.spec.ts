import { EditGroupMeetingScheduleComponent } from './edit-group-meeting-schedule.component';

describe('EditGroupMeetingScheduleComponent', () => {
  it('should create', () => {
    expect(EditGroupMeetingScheduleComponent).toBeDefined();
  });
});
