import { GroupTransferClientsComponent } from './group-transfer-clients.component';

describe('GroupTransferClientsComponent', () => {
  it('should create', () => {
    expect(GroupTransferClientsComponent).toBeDefined();
  });
});
