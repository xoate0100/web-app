import { FixedDepositsAccountActionsComponent } from './fixed-deposits-account-actions.component';

describe('FixedDepositsAccountActionsComponent', () => {
  it('should create', () => {
    expect(FixedDepositsAccountActionsComponent).toBeDefined();
  });
});
