import { AddChargeFixedDepositsAccountComponent } from './add-charge-fixed-deposits-account.component';

describe('AddChargeFixedDepositsAccountComponent', () => {
  it('should create', () => {
    expect(AddChargeFixedDepositsAccountComponent).toBeDefined();
  });
});
