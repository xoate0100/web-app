import { CloseFixedDepositsAccountComponent } from './close-fixed-deposits-account.component';

describe('CloseFixedDepositsAccountComponent', () => {
  it('should create', () => {
    expect(CloseFixedDepositsAccountComponent).toBeDefined();
  });
});
