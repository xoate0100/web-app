import { ApproveFixedDepositsAccountComponent } from './approve-fixed-deposits-account.component';

describe('ApproveFixedDepositsAccountComponent', () => {
  it('should create', () => {
    expect(ApproveFixedDepositsAccountComponent).toBeDefined();
  });
});
