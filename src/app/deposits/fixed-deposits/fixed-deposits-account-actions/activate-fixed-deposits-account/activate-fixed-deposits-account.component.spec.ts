import { ActivateFixedDepositsAccountComponent } from './activate-fixed-deposits-account.component';

describe('ActivateFixedDepositsAccountComponent', () => {
  it('should create', () => {
    expect(ActivateFixedDepositsAccountComponent).toBeDefined();
  });
});
