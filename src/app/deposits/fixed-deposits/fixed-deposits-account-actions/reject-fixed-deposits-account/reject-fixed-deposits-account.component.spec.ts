import { RejectFixedDepositsAccountComponent } from './reject-fixed-deposits-account.component';

describe('RejectFixedDepositsAccountComponent', () => {
  it('should create', () => {
    expect(RejectFixedDepositsAccountComponent).toBeDefined();
  });
});
