import { EditFixedDepositAccountComponent } from './edit-fixed-deposit-account.component';

describe('EditFixedDepositAccountComponent', () => {
  it('should create', () => {
    expect(EditFixedDepositAccountComponent).toBeDefined();
  });
});
