import { DatatableTabsComponent } from './datatable-tabs.component';

describe('DatatableTabsComponent', () => {
  it('should create', () => {
    expect(DatatableTabsComponent).toBeDefined();
  });
});
