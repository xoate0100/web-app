import { InterestRateChartTabComponent } from './interest-rate-chart-tab.component';

describe('InterestRateChartTabComponent', () => {
  it('should create', () => {
    expect(InterestRateChartTabComponent).toBeDefined();
  });
});
