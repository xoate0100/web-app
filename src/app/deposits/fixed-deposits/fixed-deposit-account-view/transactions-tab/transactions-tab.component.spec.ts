import { TransactionsTabComponent } from './transactions-tab.component';

describe('TransactionsTabComponent', () => {
  it('should create', () => {
    expect(TransactionsTabComponent).toBeDefined();
  });
});
