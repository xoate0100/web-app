import { FixedDepositAccountViewComponent } from './fixed-deposit-account-view.component';

describe('FixedDepositAccountViewComponent', () => {
  it('should create', () => {
    expect(FixedDepositAccountViewComponent).toBeDefined();
  });
});
