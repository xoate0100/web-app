import { WaiveChargeDialogComponent } from './waive-charge-dialog.component';

describe('WaiveChargeDialogComponent', () => {
  it('should create', () => {
    expect(WaiveChargeDialogComponent).toBeDefined();
  });
});
