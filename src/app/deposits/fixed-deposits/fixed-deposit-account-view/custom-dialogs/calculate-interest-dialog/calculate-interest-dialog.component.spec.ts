import { CalculateInterestDialogComponent } from './calculate-interest-dialog.component';

describe('CalculateInterestDialogComponent', () => {
  it('should create', () => {
    expect(CalculateInterestDialogComponent).toBeDefined();
  });
});
