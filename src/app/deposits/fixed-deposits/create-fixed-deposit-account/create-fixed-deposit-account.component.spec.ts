import { CreateFixedDepositAccountComponent } from './create-fixed-deposit-account.component';

describe('CreateFixedDepositAccountComponent', () => {
  it('should create', () => {
    expect(CreateFixedDepositAccountComponent).toBeDefined();
  });
});
