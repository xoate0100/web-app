import { RecurringDepositsAccountViewComponent } from './recurring-deposits-account-view.component';

describe('RecurringDepositsAccountViewComponent', () => {
  it('should create', () => {
    expect(RecurringDepositsAccountViewComponent).toBeDefined();
  });
});
