import { StandingInstructionsTabComponent } from './standing-instructions-tab.component';

describe('StandingInstructionsTabComponent', () => {
  it('should create', () => {
    expect(StandingInstructionsTabComponent).toBeDefined();
  });
});
