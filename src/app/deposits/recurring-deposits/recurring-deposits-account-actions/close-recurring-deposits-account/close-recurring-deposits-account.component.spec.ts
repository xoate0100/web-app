import { CloseRecurringDepositsAccountComponent } from './close-recurring-deposits-account.component';

describe('CloseRecurringDepositsAccountComponent', () => {
  it('should create', () => {
    expect(CloseRecurringDepositsAccountComponent).toBeDefined();
  });
});
