import { PasswordPreferencesComponent } from './password-preferences.component';

describe('PasswordPreferencesComponent', () => {
  it('should create', () => {
    expect(PasswordPreferencesComponent).toBeDefined();
  });
});
