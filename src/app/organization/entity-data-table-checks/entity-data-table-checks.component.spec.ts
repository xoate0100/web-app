import { EntityDataTableChecksComponent } from './entity-data-table-checks.component';

describe('EntityDataTableChecksComponent', () => {
  it('should create', () => {
    expect(EntityDataTableChecksComponent).toBeDefined();
  });
});
