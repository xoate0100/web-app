import { CreateCampaignComponent } from './create-campaign.component';

describe('CreateCampaignComponent', () => {
  it('should create', () => {
    expect(CreateCampaignComponent).toBeDefined();
  });
});
