import { CreateEmployeeComponent } from './create-employee.component';

describe('CreateEmployeeComponent', () => {
  it('should create', () => {
    expect(CreateEmployeeComponent).toBeDefined();
  });
});
