import { ViewBulkImportComponent } from './view-bulk-import.component';

describe('ViewBulkImportComponent', () => {
  it('should create', () => {
    expect(ViewBulkImportComponent).toBeDefined();
  });
});
