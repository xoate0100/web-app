import { EditTransactionComponent } from './edit-transaction.component';

describe('EditTransactionComponent', () => {
  it('should create', () => {
    expect(EditTransactionComponent).toBeDefined();
  });
});
