import { CloseAsRescheduledComponent } from './close-as-rescheduled.component';

describe('CloseAsRescheduledComponent', () => {
  it('should create', () => {
    expect(CloseAsRescheduledComponent).toBeDefined();
  });
});
