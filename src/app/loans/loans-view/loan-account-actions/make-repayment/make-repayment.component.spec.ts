import { MakeRepaymentComponent } from './make-repayment.component';

describe('MakeRepaymentComponent', () => {
  it('should create', () => {
    expect(MakeRepaymentComponent).toBeDefined();
  });
});
