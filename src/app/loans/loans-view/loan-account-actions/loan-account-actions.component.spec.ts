import { LoanAccountActionsComponent } from './loan-account-actions.component';

describe('LoanAccountActionsComponent', () => {
  it('should create', () => {
    expect(LoanAccountActionsComponent).toBeDefined();
  });
});
