import { WriteOffPageComponent } from './write-off-page.component';

describe('WriteOffPageComponent', () => {
  it('should create', () => {
    expect(WriteOffPageComponent).toBeDefined();
  });
});
