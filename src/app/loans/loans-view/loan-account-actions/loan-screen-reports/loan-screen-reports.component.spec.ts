import { LoanScreenReportsComponent } from './loan-screen-reports.component';

describe('LoanScreenReportsComponent', () => {
  it('should create', () => {
    expect(LoanScreenReportsComponent).toBeDefined();
  });
});
