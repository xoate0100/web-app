import { CreateGuarantorComponent } from './create-guarantor.component';

describe('CreateGuarantorComponent', () => {
  it('should create', () => {
    expect(CreateGuarantorComponent).toBeDefined();
  });
});
