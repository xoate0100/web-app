import { WaiveInterestComponent } from './waive-interest.component';

describe('WaiveInterestComponent', () => {
  it('should create', () => {
    expect(WaiveInterestComponent).toBeDefined();
  });
});
