import { AccountDetailsComponent } from './account-details.component';

describe('AccountDetailsComponent', () => {
  it('should create', () => {
    expect(AccountDetailsComponent).toBeDefined();
  });
});
