import { LoanTrancheDetailsComponent } from './loan-tranche-details.component';

describe('LoanTrancheDetailsComponent', () => {
  it('should create', () => {
    expect(LoanTrancheDetailsComponent).toBeDefined();
  });
});
