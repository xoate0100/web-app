import { OriginalScheduleTabComponent } from './original-schedule-tab.component';

describe('OriginalScheduleTabComponent', () => {
  it('should create', () => {
    expect(OriginalScheduleTabComponent).toBeDefined();
  });
});
