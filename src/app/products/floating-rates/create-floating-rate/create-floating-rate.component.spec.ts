import { CreateFloatingRateComponent } from './create-floating-rate.component';

describe('CreateFloatingRateComponent', () => {
  it('should create', () => {
    expect(CreateFloatingRateComponent).toBeDefined();
  });
});
