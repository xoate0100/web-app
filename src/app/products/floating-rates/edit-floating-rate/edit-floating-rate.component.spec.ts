import { EditFloatingRateComponent } from './edit-floating-rate.component';

describe('EditFloatingRateComponent', () => {
  it('should create', () => {
    expect(EditFloatingRateComponent).toBeDefined();
  });
});
