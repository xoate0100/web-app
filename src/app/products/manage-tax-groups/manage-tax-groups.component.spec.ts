import { ManageTaxGroupsComponent } from './manage-tax-groups.component';

describe('ManageTaxGroupsComponent', () => {
  it('should create', () => {
    expect(ManageTaxGroupsComponent).toBeDefined();
  });
});
