import { EditTaxGroupComponent } from './edit-tax-group.component';

describe('EditTaxGroupComponent', () => {
  it('should create', () => {
    expect(EditTaxGroupComponent).toBeDefined();
  });
});
