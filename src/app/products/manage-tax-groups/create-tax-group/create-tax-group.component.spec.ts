import { CreateTaxGroupComponent } from './create-tax-group.component';

describe('CreateTaxGroupComponent', () => {
  it('should create', () => {
    expect(CreateTaxGroupComponent).toBeDefined();
  });
});
