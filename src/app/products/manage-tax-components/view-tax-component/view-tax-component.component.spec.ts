import { ViewTaxComponentComponent } from './view-tax-component.component';

describe('ViewTaxComponentComponent', () => {
  it('should create', () => {
    expect(ViewTaxComponentComponent).toBeDefined();
  });
});
