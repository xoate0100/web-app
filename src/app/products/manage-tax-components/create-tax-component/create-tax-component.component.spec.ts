import { CreateTaxComponentComponent } from './create-tax-component.component';

describe('CreateTaxComponentComponent', () => {
  it('should create', () => {
    expect(CreateTaxComponentComponent).toBeDefined();
  });
});
