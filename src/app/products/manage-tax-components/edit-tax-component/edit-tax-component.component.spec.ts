import { EditTaxComponentComponent } from './edit-tax-component.component';

describe('EditTaxComponentComponent', () => {
  it('should create', () => {
    expect(EditTaxComponentComponent).toBeDefined();
  });
});
