import { ManageTaxComponentsComponent } from './manage-tax-components.component';

describe('FixedDepositProductsComponent', () => {
  it('should create', () => {
    expect(ManageTaxComponentsComponent).toBeDefined();
  });
});
