import { SavingProductsComponent } from './saving-products.component';

describe('SavingProductsComponent', () => {
  it('should create', () => {
    expect(SavingProductsComponent).toBeDefined();
  });
});
