import { ShareProductsComponent } from './share-products.component';

describe('ShareProductsComponent', () => {
  it('should create', () => {
    expect(ShareProductsComponent).toBeDefined();
  });
});
