import { CreateDividendComponent } from './create-dividend.component';

describe('CreateDividendComponent', () => {
  it('should create', () => {
    expect(CreateDividendComponent).toBeDefined();
  });
});
