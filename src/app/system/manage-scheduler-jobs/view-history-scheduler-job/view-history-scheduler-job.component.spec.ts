import { ViewHistorySchedulerJobComponent } from './view-history-scheduler-job.component';

describe('ViewHistorySchedulerJobComponent', () => {
  it('should create', () => {
    expect(ViewHistorySchedulerJobComponent).toBeDefined();
  });
});
