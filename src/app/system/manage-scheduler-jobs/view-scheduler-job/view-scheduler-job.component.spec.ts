import { ViewSchedulerJobComponent } from './view-scheduler-job.component';

describe('ViewSchedulerJobComponent', () => {
  it('should create', () => {
    expect(ViewSchedulerJobComponent).toBeDefined();
  });
});
