import { EntityToEntityMappingComponent } from './entity-to-entity-mapping.component';

describe('EntityToEntityMappingComponent', () => {
  it('should create', () => {
    expect(EntityToEntityMappingComponent).toBeDefined();
  });
});
