import { ExternalServicesComponent } from './external-services.component';

describe('ExternalServicesComponent', () => {
  it('should create', () => {
    expect(ExternalServicesComponent).toBeDefined();
  });
});
