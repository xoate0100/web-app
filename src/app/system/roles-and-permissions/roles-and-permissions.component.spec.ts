import { RolesAndPermissionsComponent } from './roles-and-permissions.component';

describe('RolesAndPermissionsComponent', () => {
  it('should create', () => {
    expect(RolesAndPermissionsComponent).toBeDefined();
  });
});
