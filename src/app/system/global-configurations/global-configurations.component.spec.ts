import { GlobalConfigurationsComponent } from './global-configurations.component';

describe('GlobalConfigurationsComponent', () => {
  it('should create', () => {
    expect(GlobalConfigurationsComponent).toBeDefined();
  });
});
