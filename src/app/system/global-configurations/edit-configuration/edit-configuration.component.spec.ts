import { EditConfigurationComponent } from './edit-configuration.component';

describe('EditConfigurationComponent', () => {
  it('should create', () => {
    expect(EditConfigurationComponent).toBeDefined();
  });
});
