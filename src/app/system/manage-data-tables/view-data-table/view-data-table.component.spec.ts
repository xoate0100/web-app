import { ViewDataTableComponent } from './view-data-table.component';

describe('ViewDataTableComponent', () => {
  it('should create', () => {
    expect(ViewDataTableComponent).toBeDefined();
  });
});
