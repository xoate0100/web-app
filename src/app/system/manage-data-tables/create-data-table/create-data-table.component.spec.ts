import { CreateDataTableComponent } from './create-data-table.component';

describe('CreateDataTableComponent', () => {
  it('should create', () => {
    expect(CreateDataTableComponent).toBeDefined();
  });
});
