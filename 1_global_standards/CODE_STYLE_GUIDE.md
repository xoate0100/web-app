# Code Style (Generic)
- Max line length 100.
- Enforce formatter per language (Prettier, Black, gofmt, dotnet format).
- Import sorting enforced.
- Header block required in all source files: file purpose + plan/task id if newly created.
